{
  "name": "react-component-statify",
  "version": "1.0.0",
  "description": "Bind a stateless component's prop to it's parent component's state.",
  "main": "./index.js",
  "scripts": {
    "compile": "tsc --sourceMap --allowSyntheticDefaultImports --declaration --module commonjs --outDir ./lib ./src/index.ts",
    "test": "npm run compile && mocha"
  },
  "keywords": [
    "react"
  ],
  "author": "syshu",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^5.0.5",
    "should": "^13.2.1"
  },
  "dependencies": {
    "@types/react": "^16.3.9"
  }
}
